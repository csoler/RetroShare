#include <string>

/*!
 * Generates a random SVG avatar from the string passed in parameter.
 *
 * s:   	string to copute the avatar from
 * sansEnv: 	asks for an avatar without background
 *
 * Returns: Avatar code in svg format.
 */

std::string multiavatar(const std::string& s,bool sansEnv);

