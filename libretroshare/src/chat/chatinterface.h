// The ChatInterface represents a object that sends chat to be displayed and accepts chat to be sent 
// to other participants
//
class ChatInterface
{
	public:
		ChatInterface() {}

		sendChat();

		// Retrieves the history for that interface.
		//
		void getChatHistory() ;
};
